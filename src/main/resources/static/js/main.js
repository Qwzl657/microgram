'use strict';

window.addEventListener('load', function () {

    initLikes();

    initSubscribe();

    initImagePreview();

    initDescriptionCounter();

    initDeleteConfirm();

    initLiveSearch();

});